export enum PlanStatus {
  READY = 'ready',
  INCOMPLETE = 'incomplete',
  EXPIRED = 'expired',
}

export enum PlanType {
  MARKET = 'MARKET',
  STANDING = 'STANDING',
}

export enum CustomerType {
  RESI = 'RESI',
  BUS = 'BUS',
}
